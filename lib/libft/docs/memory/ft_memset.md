# ft_memset

## Description

Remplit une zone mémoire avec un octet constant

## Prototype

```c
void *ft_memset(void *s, int c, size_t n);
```

## Paramètres

| Paramètre | Type | Description |
|-----------|------|-------------|
| `s` | `void *` | Pointeur vers la zone mémoire |
| `c` | `int` | Valeur à écrire |
| `n` | `size_t` | Nombre d'octets |


## Valeur de retour

Un pointeur vers la zone mémoire s

## Description détaillée

La fonction `ft_memset` remplit une zone mémoire avec un octet constant

## Exemple d'utilisation

```c
#include "includes/libft.h"
#include <stdio.h>

int main(void)
{
    // Exemple d'utilisation de ft_memset
    // TODO: Ajoutez votre code ici
    return 0;
}
```

## Notes importantes

⚠️ **Attention** : Consultez la section paramètres pour les préconditions.

## Voir aussi

Consultez les autres fonctions de la catégorie dans la barre latérale.
